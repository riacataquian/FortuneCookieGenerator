= render "fortune_cookie.html", fortune_cookie: @fortune_cookie
.row
  .content-block.col-lg-12
    h4 Of course, you can change your fortune!
    = render "form.html", changeset: @changeset, action: fortune_path(@conn, :update, @fortune_cookie)

  = link to: fortune_path(@conn, :random), class: "link -warm -navigation-left" do
    | Back
